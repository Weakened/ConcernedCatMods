# Concerned Teamster cart load calibration data (CT-008).
# Versioned data, not code: the LoadModel derives every verdict from the
# rows below and answers "unknown" outside their coverage.
#
# Format:
#   header lines   key: value
#   data rows      row: <grade-percent> | <total-cart-mass> | <outcome> | <basis> | <note>
# Outcomes: Climbs | Marginal | Stalls | JointBreak
# Bases:    Measured (protocol runs only) | Prior (labeled assumption)
#           | DerivedConstant (computed from decompile-verified constants)
#
# Provenance: no Measured rows exist yet — in-game protocol runs are
# owner/manual work pending the TCT profiles (CT-043). Priors and derived
# bounds are labeled as exactly what they are; see the protocol document.

data-version: 1
game-version: 0.221.12
game-buildid: 21981559
protocol: docs/mods/concerned-teamster/CALIBRATION_PROTOCOL.md
generated: 2026-09-04

# Flat-ground pullability priors (vanilla design intent: carts are
# routinely pulled on flat ground; sets A-C of the protocol).
row: 0 | 20 | Climbs | Prior | set A empty cart on flat; protocol confirmation pending
row: 0 | 70 | Climbs | Prior | set B on flat; protocol confirmation pending
row: 0 | 220 | Climbs | Prior | set C on flat; protocol confirmation pending

# Physical impossibility bounds from decompile-verified constants
# (m_breakForce = 10000): holding the cart statically on the slope already
# exceeds the joint's break force (force >= mass * g * sin(atan(grade/100))),
# independent of player strength. Gravity itself is not read from the game,
# so each bound states the minimum gravity for which it holds — Unity's
# default is 9.81, far above every threshold used here.
row: 30 | 8000 | JointBreak | DerivedConstant | breaks for g >= 4.4 m/s^2 (8000 * 4.4 * 0.2873 > 10000)
row: 45 | 6000 | JointBreak | DerivedConstant | breaks for g >= 4.1 m/s^2 (6000 * 4.1 * 0.4103 > 10000)
